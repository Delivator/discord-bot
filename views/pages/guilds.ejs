<!DOCTYPE html>
<html lang="en">
<head>
  <% include ../partials/head %>
  <title><%= title %></title>
</head>
<body>
  <header>
    <% include ../partials/header %>
  </header>

  <div class="row">
    <div class="col s12">
      <div class="container">
        <br>
        <h2 class="center"><%= guild.name %></h2>
        <div class="row">
          <div class="col s12 m6 xl4">
            <a href="members/">
              <div class="card hoverable black-text">
                <div class="card-content">
                  <span class="card-title">Members</span>
                  <p><%= members.total %> Users on this server (<%= members.online %> online, <%= members.offline %> offline)</p>
                </div>
              </div>
            </a>
          </div>
          <div class="col s12 m6 xl4">
            <a href="music/">
              <div class="card hoverable black-text">
                <div class="card-content">
                  <span class="card-title">Music</span>
                  <% if(queue[0]) { %>
                    <p class="nowplaying"><%= queue[0].title %></p>
                  <% } else { %>
                    <p class="nowplaying">No music playing</p>
                  <% } %>
                </div>
              </div>
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
  <% include ../partials/footer %>
</body>
</html>